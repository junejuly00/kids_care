<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kcuserpost">

	<sql id="file">
		SELECT  kcuserpost.*, userfile.code,userfile.userPostId pid, userfile.filename, userfile.uuid  
		FROM kcuserpost LEFT JOIN userfile
		ON kcuserpost.userPostId=userfile.userPostId
		ORDER BY kcuserpost.userpostid
	</sql>

	<select id="list" resultMap="KcUserPostMap">
		SELECT * FROM
			( SELECT sub.*, ROWNUM rnum FROM
				(<include refid="file"></include>) sub)
		<if test="perPage != 0">
		<![CDATA[		
		WHERE rnum BETWEEN (((#{page}-1) * #{perPage})+1) AND (#{page} * #{perPage})
		AND status=0
		]]>
		</if>
	</select>
	
	<resultMap type="KcUserPost" id="KcUserPostMap" autoMapping="true">
		<id column="userPostId" property="userPostId"/>
		
		<collection property="userFiles" column="pid" javaType="ArrayList" ofType="UserFile" autoMapping="true">
			<id column="code" property="code"/>
		</collection>
	</resultMap>

	<select id="total" resultType="Integer">
		SELECT COUNT(*) FROM (
		SELECT * FROM kcuserpost
		ORDER BY userpostid
		) sub
	</select>
	
	<insert id="add">
	    INSERT into kcuserpost
	    VALUES(#{userPostId},#{username},sysdate,#{title},#{contents},0)
	</insert>
	
	<update id="update">
	    UPDATE kcuserpost
	    SET username=#{username},title=#{title},contents=#{contents}
	    WHERE userPostId=#{userPostId}
	</update>
	


	<select id="search" resultMap="KcUserPostMap">
		SELECT *
		FROM kcuserpost LEFT JOIN userfile ON kcuserpost.userpostid=userfile.userpostid
		WHERE kcuserpost.userPostId=#{userPostId}

	</select>
	
	<update id="hide">
		UPDATE kcuserpost
		SET status=1
		where userPostId=#{userPostId}
	</update>
	
	<insert id="add_image">
		INSERT INTO userfile (code, userPostId, filename, uuid)
		VALUES (SEQ_code.nextval, #{userPostId},#{filename},#{uuid})
	</insert>
</mapper>

